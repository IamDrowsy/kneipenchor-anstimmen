{
  "name": "kneipenchor-anstimmen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-songs": "node build-songs.js",
    "build": "npm run build-songs && vite build",
    "deploy": "node deploy.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "basic-ftp": "^5.0.5",
    "vite": "^6.2.5"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "tone": "^15.0.4"
  }
}
